apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - traefik-config.yaml
  # Already created by default in Treafik when Gateway is enabled
  # - gateway.yaml
  - experimental-install.yaml